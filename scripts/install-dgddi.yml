commands:
  # Drop previus
  - command: database:drop
  # Install Drupal
  - command: site:install
    options:
      langcode: fr
      db-type: postgres
      #db-file: ../config/drupal/local/.ht.postrgres
      db-host: postgres
      db-name: dgddi
      db-user: dbusr
      db-pass: dbpwd
      db-prefix: drupal_
      db-port: 5432
      site-name: 'Gestion de contenu DGDDI'
      site-mail: do-not-replay@dgddi.fr
      account-name: admin
      account-mail: admin@example.com
      account-pass: admin
    arguments:
      profile: minimal